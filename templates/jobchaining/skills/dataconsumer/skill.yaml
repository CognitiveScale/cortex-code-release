camel: 1.0.0
name: {{dashCase skillname}}-dataconsumer
title: Data Consumer job skill
description: ...
inputs:
  - name: input
    title: input
    routing:
      all:
        action: dataconsumer-action
        output: output
        runtime: cortex/jobs
outputs:
  - name: output
    title: output
actions:
  - name: dataconsumer-action
    description: {{skillname}} DataConsumer Action
    image: {{repo.url}}/{{dashCase skillname}}-dataconsumer-action
    command: []
    port: "8080"
    type: daemon
    scaleCount: 1
    environmentVariables: ""
